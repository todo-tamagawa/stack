sudo: required

language: generic

service: docker

# before_install:
#   - docker build -t ix:builder environment/
#   - docker run -itd --name builder -p 53001:53001 ix:builder
#   - docker cp ix builder:/root/dev/ix

script:
  - stack --no-terminal --skip-ghc-check test
